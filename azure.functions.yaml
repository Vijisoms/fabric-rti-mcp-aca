# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: self-hosted-mcp-azd-python
metadata:
  template: self-hosted-mcp-azd-python@1.0.0
services:
  mcp:
    project: .
    language: python
    host: function
